//! The semantics surface: what a table's columns *mean*.
//!
//! The resolved view over every producer's and every person's opinion rows
//! in the store, plus the detector that gives an undescribed table its base
//! layer.

pub mod detect;
pub mod handlers;
pub mod types;
