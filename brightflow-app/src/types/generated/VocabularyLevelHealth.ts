// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { TooSmallEntry } from "./TooSmallEntry";
import type { UnbalancedEntry } from "./UnbalancedEntry";

/**
 * One vocabulary level's health (see the engine's `vocabulary::health`).
 */
export type VocabularyLevelHealth = { 
/**
 * category | subcategory | feedback_category | product | competitor
 */
kind: string, entries: number, cap: number, rows: number, 
/**
 * Share of rows in `other`; above ~0.15 the vocabulary is wrong.
 */
otherRate: number, maxShare: number, minShare: number, 
/**
 * Entries outside the 2 %–40 % balance band, with their share.
 */
unbalanced: Array<UnbalancedEntry>, 
/**
 * Entries with rows but fewer than `min_rows_per_entry` — items, not
 * groups. Zero-row entries are reported as unbalanced instead.
 */
tooSmall: Array<TooSmallEntry>, };
